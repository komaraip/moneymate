export function LoginPlaceholder() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-950 px-5 text-zinc-100">
      <section className="w-full max-w-md rounded-xl border border-zinc-800 bg-zinc-900/70 p-6">
        <p className="text-xs font-semibold uppercase tracking-[0.24em] text-emerald-300">
          MoneyMate
        </p>
        <h1 className="mt-3 text-2xl font-semibold text-white">Login</h1>
        <p className="mt-3 text-sm leading-6 text-zinc-400">
          Form login lengkap akan diisi pada task frontend MVP berikutnya.
        </p>
      </section>
    </main>
  );
}
