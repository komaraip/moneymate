import { test } from "@playwright/test";

test.describe("review flow", () => {
  test.skip("uploads a stock statement, resolves review items, approves the document, and shows approved holdings", async () => {
    // E2E scaffold intentionally left for when local Node + browser tooling is available.
  });
});
