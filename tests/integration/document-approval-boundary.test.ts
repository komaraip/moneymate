import { describe, it } from "vitest";

describe("document approval boundary", () => {
  it.todo("excludes pending trade activities and holding snapshots from dashboard and holdings queries");
  it.todo("moves pending parsed fields and linked investment records to approved when a document is approved");
});

